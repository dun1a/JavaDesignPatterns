package Example.view;


// this is the Subscriber interface that declares the notification interface
// consists of a single update() method (may have many parameters)
public interface Observer {

    void update();
}
